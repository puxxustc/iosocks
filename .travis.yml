language: c
compiler:
    - gcc
    - clang
before_install:
    - sudo apt-get update -qq
install:
    - sudo apt-get install -y libev-dev
before_script:
    - "autoreconf -i"
script:
    - "./configure && make"
branches:
    only:
        - master