language: c
compiler:
    - gcc
    - clang
before_install:
    - sudo apt-get update -qq
install:
    - sudo apt-get install -y libev-dev
before_script:
    - "sed -i -e s/2\.69/2\.67/g configure.ac"
    - "autoreconf -i"
script:
    - "./configure && make"
branches:
    only:
        - master